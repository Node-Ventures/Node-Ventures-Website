{
  "name": "node-ventures-site",
  "version": "1.0.0",
  "private": true,
  "description": "Node Ventures marketing site — static, pre-compiled JSX (no runtime Babel).",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@babel/core": "^7.25.0",
    "@babel/preset-react": "^7.24.0"
  }
}
